/**
 * Class to map GeoJSON input onto a map.
 */
export class MapInput {

    /**
     * Maps a list of input from a user onto a map
     *
     * @param inputArray object array of inputted points from a user to be mapped.
     * Each object should have a key for an (optional) label, and a GeoJSON object
     */
    public mapInputArray(inputArray : object[]) : void {

    }

    private mapInput(input : object) : void {

    }


}